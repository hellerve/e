#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int isnum(char*);
int issep(char);
char* strsub(char*, char*, char*);
char* strtriml(char*);
int strcmpr(char*, char*);
char fpeek(FILE*);
